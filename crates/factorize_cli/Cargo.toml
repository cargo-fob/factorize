[package]
name = "factorize_cli"
version.workspace = true
edition.workspace = true

[[bin]]
name = "factorize"
path = "src/main.rs"

[dependencies]
factorize_core = { path = "../factorize_core" }
task_loop = { path = "../task_loop" }
tokio = { version = "1", features = ["full"] }
